<p>
  <strong>Login:</strong>
  <%= @user.login %>
</p>

<p>
  <strong>First name:</strong>
  <%= @user.first_name %>
</p>

<p>
  <strong>Last name:</strong>
  <%= @user.last_name %>
</p>

<%= render @posts %>

<div>
  <%= link_to 'Edit User', edit_user_path(@user) if can? :update, @user %>
  <%= link_to 'Back to users', users_path %>
</div>
